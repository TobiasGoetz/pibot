image:
  repository: ghcr.io/tobiasgoetz/pibot
  tag: ""

# Pass at install: --set discord.token=... --set mongodb.uri=... --set deepl.apiKey=...
discord:
  token: ""
mongodb:
  uri: ""
deepl:
  apiKey: ""

logging:
  level: INFO

bot:
  settings:
    commandSyncBehavior: global
    enableDevTools: false
